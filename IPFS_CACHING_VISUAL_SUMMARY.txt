╔══════════════════════════════════════════════════════════════════════════════╗
║                IPFS CACHING INFRASTRUCTURE - VISUAL SUMMARY                  ║
║                          AgentSphere Backend                                  ║
║                            2026-01-24                                         ║
╚══════════════════════════════════════════════════════════════════════════════╝

┌──────────────────────────────────────────────────────────────────────────────┐
│                          ARCHITECTURE OVERVIEW                               │
└──────────────────────────────────────────────────────────────────────────────┘

    ┌─────────────────────────────────────────────────────────────────────┐
    │              ipfs-storage Container (supervisord)                    │
    │                                                                      │
    │   ┌──────────────────┐   ┌──────────────────┐   ┌───────────────┐  │
    │   │     nginx        │   │   IPFS Kubo      │   │ nostr-pinner  │  │
    │   │   :443 HTTPS     │───│   :5001 API      │───│   :9081 HTTP  │  │
    │   │   :4003 WSS      │   │   :8080 Gateway  │   │   SQLite DB   │  │
    │   └──────────────────┘   └──────────────────┘   └───────────────┘  │
    │          │                        │                      │           │
    │    TLS Termination          DHT + Bitswap       IPNS Cache Layer    │
    │    HTTP Caching             Content Storage      5-20ms lookups     │
    │    Fast-path routing        Peer connections     Sequence validation│
    └─────────────────────────────────────────────────────────────────────┘
              │                        │                      │
              ▼                        ▼                      ▼
        Browser Clients          IPFS Network         Nostr Relays
        (Helia, curl)            (DHT, libp2p)        (pin announcements)


┌──────────────────────────────────────────────────────────────────────────────┐
│                        TWO-TIER CACHING SYSTEM                               │
└──────────────────────────────────────────────────────────────────────────────┘

╔════════════════════════════════════════════════════════════════════════════╗
║ TIER 1: Nginx HTTP Cache                                                   ║
║ ─────────────────────────                                                  ║
║ Location:  /var/cache/nginx/ipfs                                           ║
║ Size:      100MB metadata + 10GB content                                   ║
║ TTL:       • /ipfs/ → 7 days (immutable content)                           ║
║            • /ipns/ → 30 seconds (mutable IPNS records)                    ║
║                                                                             ║
║ Features:                                                                   ║
║   ✓ Stale cache serving on upstream errors                                 ║
║   ✓ Background cache updates                                               ║
║   ✓ Cache bypass via X-No-Cache header                                     ║
║   ✓ Gzip disabled for CID verification integrity                           ║
╚════════════════════════════════════════════════════════════════════════════╝
                                    │
                                    ▼
╔════════════════════════════════════════════════════════════════════════════╗
║ TIER 2: Sidecar SQLite Database                                            ║
║ ────────────────────────────────                                           ║
║ Location:  /data/ipfs/propagation.db                                       ║
║ Service:   nostr-pinner.py (Python, port 9081)                             ║
║                                                                             ║
║ Tables:                                                                     ║
║   • pinned_cids: Tracks pinned content with announcement metadata          ║
║   • ipns_records: Stores IPNS records with sequence validation             ║
║   • metrics: Service statistics                                            ║
║                                                                             ║
║ Features:                                                                   ║
║   ✓ 5-20ms IPNS lookups (vs 1-5 sec DHT)                                   ║
║   ✓ Sequence-based validation (prevents stale records)                     ║
║   ✓ Dual-source queries (DB + Kubo, returns highest sequence)              ║
║   ✓ Automatic cache updates on publish (nginx mirror)                      ║
║   ✓ Periodic re-announcement to DHT                                        ║
╚════════════════════════════════════════════════════════════════════════════╝


┌──────────────────────────────────────────────────────────────────────────────┐
│                  IPNS RESOLUTION FLOW (FAST PATH)                            │
└──────────────────────────────────────────────────────────────────────────────┘

    Browser: GET /api/v0/routing/get?arg=/ipns/{nametag}
                            │
                            ▼
                    ┌───────────────┐
                    │  nginx :443   │  Location: /api/v0/routing/get
                    └───────────────┘
                            │
                ┌───────────┴───────────┐
                │                       │
                │   TRY SIDECAR (1-2s timeout)
                │                       │
                ▼                       │
        ┌───────────────────┐           │
        │  Sidecar :9081    │           │
        │  SQLite Query     │           │
        │  SELECT record    │           │
        │  WHERE name = ?   │           │
        └───────────────────┘           │
                │                       │
        ┌───────┴────────┐              │
        │                │              │
    CACHE HIT       CACHE MISS          │
    (5-20ms)        (404)               │
        │                │              │
        │                ▼              │
        │        ┌────────────────┐     │
        │        │ Kubo DHT :5001 │     │
        │        │ Lookup (1-5s)  │     │
        │        └────────────────┘     │
        │                │              │
        └────────────────┘              │
                │                       │
                ▼                       ▼
        ┌───────────────────────────────┐
        │  Dual-source validation:      │
        │  Compare DB seq vs Kubo seq   │
        │  Return record with highest   │
        └───────────────────────────────┘
                        │
                        ▼
        ┌─────────────────────────────────────┐
        │ Response Headers:                   │
        │ X-IPNS-Source: sidecar-cache|kubo   │
        │ X-IPNS-Sequence: 42                 │
        │ X-Cache-Status: HIT|MISS            │
        └─────────────────────────────────────┘


┌──────────────────────────────────────────────────────────────────────────────┐
│                  IPNS RECORD INTERCEPTION (PUBLISH)                          │
└──────────────────────────────────────────────────────────────────────────────┘

    Browser: POST /api/v0/routing/put?arg=/ipns/{name}
             Body: multipart/form-data with IPNS record (protobuf)
                            │
                            ▼
                    ┌───────────────┐
                    │  nginx :443   │  Location: /api/v0/routing/put
                    └───────────────┘
                            │
                ┌───────────┴───────────────┐
                │                           │
         ┌──────▼──────┐           ┌───────▼─────────┐
         │  MIRROR to  │           │   FORWARD to    │
         │  Sidecar    │           │   Kubo DHT      │
         └──────┬──────┘           └───────┬─────────┘
                │                           │
                ▼                           ▼
    ┌────────────────────────┐  ┌────────────────────────┐
    │ Sidecar :9081          │  │ Kubo :5001             │
    │ /ipns-intercept        │  │ Publish to DHT         │
    │                        │  │                        │
    │ 1. Parse protobuf      │  │ 1. Validate signature  │
    │ 2. Extract sequence    │  │ 2. Publish to network  │
    │ 3. Validate seq >= old │  │ 3. Propagate to peers  │
    │ 4. Store in SQLite     │  │                        │
    └────────────────────────┘  └────────────────────────┘
                │                           │
                ▼                           ▼
    SQLite DB updated           DHT network updated
    (for future fast reads)     (for global resolution)


┌──────────────────────────────────────────────────────────────────────────────┐
│                        PERFORMANCE COMPARISON                                │
└──────────────────────────────────────────────────────────────────────────────┘

╔═══════════════════════╦══════════════╦═══════════════╦═════════════════════╗
║ Operation             ║ Cold (DHT)   ║ Warm (Sidecar)║ Speedup             ║
╠═══════════════════════╬══════════════╬═══════════════╬═════════════════════╣
║ IPNS resolution       ║ 1-5 seconds  ║ 5-20ms        ║ 250x faster ⚡⚡⚡  ║
║ IPFS content fetch    ║ 500ms-2s     ║ 10-50ms       ║ 50x faster ⚡⚡     ║
║ Pin status check      ║ 100-200ms    ║ 5ms           ║ 40x faster ⚡⚡     ║
║ Token sync (10 tokens)║ 10-30 sec    ║ 200-600ms     ║ 50x faster ⚡⚡⚡   ║
╚═══════════════════════╩══════════════╩═══════════════╩═════════════════════╝

Cache Hit Rate (Expected): 80-95% for repeated IPNS lookups


┌──────────────────────────────────────────────────────────────────────────────┐
│                    TOKEN SYNC FLOW BREAKDOWN                                 │
└──────────────────────────────────────────────────────────────────────────────┘

WITHOUT CACHING (Current):
┌──────────────────────────┐
│ 1. Resolve IPNS nametag  │  1-5 seconds    (DHT lookup)
│ 2. Fetch IPNS CID        │  500ms-2s       (bitswap)
│ 3. Fetch 10 token CIDs   │  5-20 seconds   (10x bitswap)
│────────────────────────────────────────────────────────
│ TOTAL:                   │  10-30 seconds  ❌ SLOW
└──────────────────────────┘

WITH SIDECAR CACHING (Optimized):
┌──────────────────────────┐
│ 1. Resolve IPNS nametag  │  5-20ms         (SQLite lookup) ⚡
│ 2. Fetch IPNS CID        │  10-50ms        (nginx cache) ⚡
│ 3. Fetch 10 token CIDs   │  100-500ms      (nginx cache) ⚡
│────────────────────────────────────────────────────────
│ TOTAL:                   │  200-600ms      ✅ 50x FASTER
└──────────────────────────┘

Note: First sync is always slow (cold cache). Subsequent syncs are nearly instant.


┌──────────────────────────────────────────────────────────────────────────────┐
│                         SEQUENCE VALIDATION                                  │
└──────────────────────────────────────────────────────────────────────────────┘

IPNS Record Structure (protobuf):
┌────────────────────────────────────────┐
│ field 1 (bytes): value (/ipfs/bafyXXX) │  ← CID pointer
│ field 3 (varint): validity_type (EOL)  │
│ field 5 (varint): sequence             │  ← Version number
│ field 6 (varint): ttl                  │
└────────────────────────────────────────┘

Sidecar Validation Logic:
┌──────────────────────────────────────────────────────────────┐
│ def store_record(ipns_name, record_bytes):                  │
│     new_seq = parse_sequence(record_bytes)                  │
│     existing_seq = db.get_sequence(ipns_name)               │
│                                                              │
│     if new_seq < existing_seq:                              │
│         return False  # ❌ Reject stale record               │
│                                                              │
│     db.insert_or_replace(ipns_name, record_bytes, new_seq)  │
│     return True  # ✅ Stored                                 │
└──────────────────────────────────────────────────────────────┘

Dual-Source Query:
┌──────────────────────────────────────────────────────────────┐
│ def handle_routing_get(ipns_name):                          │
│     db_seq = query_sidecar_db(ipns_name)                    │
│     kubo_seq = query_kubo_dht(ipns_name)                    │
│                                                              │
│     if kubo_seq > db_seq:                                   │
│         update_sidecar_cache(kubo_record)                   │
│         return kubo_record  # Kubo has newer                │
│     else:                                                    │
│         return db_record    # Sidecar is fresh/equal        │
└──────────────────────────────────────────────────────────────┘

Result: Always returns record with HIGHEST sequence number (most recent)


┌──────────────────────────────────────────────────────────────────────────────┐
│                         INTEGRATION CHECKLIST                                │
└──────────────────────────────────────────────────────────────────────────────┘

Current Sphere App (IpfsHttpResolver.ts):
  ❌ Uses HTTP gateway:  /ipns/{nametag}
  ❌ Bypasses sidecar cache
  ❌ No cache hit rate tracking

Recommended Changes:
  ✅ Use routing API:    /api/v0/routing/get?arg=/ipns/{nametag}
  ✅ Parse IPNS record:  base64Decode(response.Extra)
  ✅ Track cache hits:   response.headers['X-IPNS-Source']
  ✅ Monitor sequence:   response.headers['X-IPNS-Sequence']

Code Example:
┌──────────────────────────────────────────────────────────────────────────────┐
│ // BEFORE (slow):                                                            │
│ const url = `${gateway}/ipns/${nametag}`;                                   │
│ const response = await fetch(url);                                          │
│                                                                              │
│ // AFTER (fast):                                                             │
│ const url = `${gateway}/api/v0/routing/get?arg=/ipns/${nametag}`;           │
│ const response = await fetch(url);                                          │
│ const data = await response.json();                                         │
│ const record = base64Decode(data.Extra);                                    │
│ const cid = parseIpnsRecord(record).value;                                  │
│                                                                              │
│ // Monitor cache:                                                            │
│ const source = response.headers.get('X-IPNS-Source');                       │
│ console.log(`IPNS resolved from ${source}`);                                │
└──────────────────────────────────────────────────────────────────────────────┘


┌──────────────────────────────────────────────────────────────────────────────┐
│                         MONITORING ENDPOINTS                                 │
└──────────────────────────────────────────────────────────────────────────────┘

Sidecar HTTP API (port 9081, internal):
┌────────────────────────────────────────────────────────────────────────────┐
│ GET  /routing-get?arg=/ipns/{name}  → Fast IPNS lookup (5-20ms)           │
│ GET  /pin-status?cid={cid}          → Check pin status (5ms)              │
│ GET  /metrics                       → Service statistics (JSON)           │
│ GET  /health                        → Health check (<1ms)                 │
│ POST /reannounce                    → Manual re-publish trigger           │
└────────────────────────────────────────────────────────────────────────────┘

Example Metrics Response:
┌────────────────────────────────────────────────────────────────────────────┐
│ {                                                                          │
│   "database": {                                                            │
│     "total_pinned_cids": 1523,      // Pins stored                        │
│     "total_ipns_records": 42,       // IPNS records cached                │
│     "ipns_announcements": 8         // Re-publish count                   │
│   },                                                                       │
│   "session": {                                                             │
│     "cids_pinned": 1500,            // Successful pins                    │
│     "ipns_records_stored": 42,      // New records this session           │
│     "reannouncements": 3            // Re-publish cycles                  │
│   }                                                                        │
│ }                                                                          │
└────────────────────────────────────────────────────────────────────────────┘


┌──────────────────────────────────────────────────────────────────────────────┐
│                         KEY CONFIGURATION                                    │
└──────────────────────────────────────────────────────────────────────────────┘

Kubo Performance Tuning (configure-ipfs.sh):
┌────────────────────────────────────────────────────────────────────────────┐
│ ipfs config --json Routing.AcceleratedDHTClient true    # CRITICAL        │
│ ipfs config --json Ipns.ResolveCacheSize 4096          # 4K IPNS cache    │
│ ipfs config --json Datastore.BloomFilterSize 1048576   # 1MB bloom        │
│ ipfs config --json Gateway.ExposeRoutingAPI true       # Enable API       │
└────────────────────────────────────────────────────────────────────────────┘

Nginx Cache Configuration (nginx.conf.template):
┌────────────────────────────────────────────────────────────────────────────┐
│ proxy_cache_path /var/cache/nginx/ipfs                                    │
│     keys_zone=ipfs_cache:100m                                             │
│     max_size=10g                                                          │
│     inactive=7d;                                                          │
│                                                                            │
│ location /ipfs/ {                                                          │
│     proxy_cache_valid 200 7d;      # 7-day TTL (immutable)                │
│     gzip off;                      # CRITICAL for CID verification        │
│ }                                                                          │
│                                                                            │
│ location /ipns/ {                                                          │
│     proxy_cache_valid 200 30s;     # 30-second TTL (mutable)              │
│     proxy_cache_bypass $http_x_no_cache;  # Allow cache bypass            │
│     gzip off;                      # CRITICAL for CID verification        │
│ }                                                                          │
└────────────────────────────────────────────────────────────────────────────┘

Sidecar Environment Variables (docker-compose.yml):
┌────────────────────────────────────────────────────────────────────────────┐
│ DB_PATH=/data/ipfs/propagation.db                                         │
│ MAX_PINS_PER_SECOND=100                                                   │
│ ANNOUNCE_INTERVAL=0              # 0 = probabilistic (~1/hour)            │
│ ANNOUNCE_PROBABILITY=0.000277778 # 1/3600 seconds                         │
│ NOSTR_RELAYS=wss://nostr-relay.testnet.unicity.network,...                │
└────────────────────────────────────────────────────────────────────────────┘


┌──────────────────────────────────────────────────────────────────────────────┐
│                         SECURITY FEATURES                                    │
└──────────────────────────────────────────────────────────────────────────────┘

✓ Sequence Validation:     Prevents replay attacks (only seq >= existing)
✓ Dual-Source Queries:     Always validates against Kubo authority
✓ CID Verification:        Gzip disabled for byte-exact integrity
✓ Rate Limiting:           Max 100 pins/sec prevents DoS
✓ Format Validation:       Rejects malformed CIDs/IPNS names
✓ Deduplication:           Prevents duplicate pin queue entries


┌──────────────────────────────────────────────────────────────────────────────┐
│                         DEPLOYMENT STATUS                                    │
└──────────────────────────────────────────────────────────────────────────────┘

Backend Infrastructure:
  ✅ IPFS caching service deployed at /home/vrogojin/ipfs-storage
  ✅ Nginx configured with fast-path routing
  ✅ Sidecar SQLite database operational
  ✅ Nostr integration for pin announcements
  ✅ CID verification fix implemented (gzip off)

Sphere App Integration:
  🔄 Pending: Update IpfsHttpResolver.ts to use routing API
  🔄 Pending: Add cache hit rate monitoring
  🔄 Pending: Performance testing and validation

Expected Improvement:
  Token sync: 10-30 seconds → 200-600ms (50x faster)


┌──────────────────────────────────────────────────────────────────────────────┐
│                         NEXT STEPS                                           │
└──────────────────────────────────────────────────────────────────────────────┘

1. ✅ Understand caching architecture (COMPLETE)
2. 🔄 Update IpfsHttpResolver.ts:
      - Replace /ipns/ gateway calls with /api/v0/routing/get
      - Parse IPNS records from response.Extra (base64)
      - Extract CID from parsed record
3. 🔄 Add monitoring:
      - Track X-IPNS-Source header distribution
      - Log cache hit rate
      - Alert on high Kubo fallback rate
4. 🔄 Test performance:
      - Measure first sync (cold cache)
      - Measure subsequent syncs (warm cache)
      - Validate 50x speedup achieved
5. 🔄 Production deployment:
      - Deploy updated Sphere app
      - Monitor metrics for 24 hours
      - Document performance improvements


╔══════════════════════════════════════════════════════════════════════════════╗
║                             KEY TAKEAWAYS                                    ║
╠══════════════════════════════════════════════════════════════════════════════╣
║                                                                              ║
║  1. Sidecar provides 250x speedup for IPNS resolution (5-20ms vs 1-5 sec)   ║
║  2. Dual-source queries ensure you always get the latest record             ║
║  3. Use /api/v0/routing/get instead of /ipns/ HTTP gateway                  ║
║  4. Automatic fallback to DHT if cache miss (transparent to client)         ║
║  5. Expected result: Token sync 10-30s → 200-600ms (50x faster)             ║
║                                                                              ║
╚══════════════════════════════════════════════════════════════════════════════╝

Documentation Files:
  • IPFS_CACHING_INFRASTRUCTURE_ANALYSIS.md  (full technical analysis)
  • IPFS_CACHING_QUICK_REFERENCE.md           (integration guide)
  • IPFS_CACHING_VISUAL_SUMMARY.txt           (this file)

Backend Code:
  • /home/vrogojin/ipfs-storage/                  (infrastructure)
  • /home/vrogojin/ipfs-storage/config/nginx.conf.template  (nginx config)
  • /home/vrogojin/ipfs-storage/nostr-pinner/nostr_pinner.py  (sidecar)

Status: ✅ Research complete, ready for integration

═══════════════════════════════════════════════════════════════════════════════
