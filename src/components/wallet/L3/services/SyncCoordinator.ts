/**
 * SyncCoordinator - Tab coordination for IPFS sync operations
 *
 * Re-exports from SDK browser module for backwards compatibility.
 * @see sdk/browser/sync-coordinator.ts for implementation
 */

export {
  SyncCoordinator,
  getSyncCoordinator,
  type SyncCoordinatorConfig,
} from "../../sdk/browser/sync-coordinator";
